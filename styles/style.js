import { StyleSheet } from "react-native";

export const gStyles = StyleSheet.create({
  main: {
    flex: 1,
    padding: 20,
    textAlign: "center",
  },
  title: {
    fontSize: 20,
    color: "#333",
    textAlign: "center",
  },
});
